# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
bundle-stats.html
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# -----------------------------------------------------------------------------
# Secrets & environnement — ne jamais committer (CI / hôte : variables dédiées)
# -----------------------------------------------------------------------------
.env
.env.*
!.env.example
!.env.sample

# Mapping UUID ancien → nouveau projet (données sensibles / spécifiques machine)
user-id-map.json
user-id-map.*.json

# Fichiers Supabase CLI locaux (tokens, état link)
.supabase/

# Cache / métadonnées CLI (ne pas versionner — ref projet dans config.toml + .env)
supabase/.temp/

# Dumps / sauvegardes JSON (peuvent contenir PII ou clés dans les URLs)
# NB : scripts/ reste versionné ; pas de secrets dans le repo (variables .env).
.tmp-db-reimport
*.backup.json
backups/
backup_*.json

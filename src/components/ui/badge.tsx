/**
 * Réexport — les styles suivent **Material Design 3** (chips : 32×8dp, label medium).
 * Pour les nouveaux usages préférer `Chip` depuis `@/components/ui/chip`.
 */
export { Badge, badgeVariants, type BadgeProps } from "@/components/ui/chip";
